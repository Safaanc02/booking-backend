package com.example.booking.model.enums;

public enum OrigineReservation {
    /** Le client a réservé lui-même sur le site. */
    EN_LIGNE,
    /** Saisie par le salon après un appel. */
    TELEPHONE,
    /** Saisie au comptoir, client présent. */
    COMPTOIR
}
