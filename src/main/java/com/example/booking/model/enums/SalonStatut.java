package com.example.booking.model.enums;

public enum SalonStatut {
    /** Créé par le professionnel, en attente de validation par la plateforme. */
    EN_ATTENTE,
    /** Visible publiquement et réservable. */
    ACTIF,
    /** Retiré de la recherche sans être supprimé. */
    SUSPENDU
}
